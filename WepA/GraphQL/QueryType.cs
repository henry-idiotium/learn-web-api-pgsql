using HotChocolate.Types;

namespace WepA.GraphQL
{
	public class QueryType
	// : ObjectType<Query>
	{
		// protected override void Configure(IObjectTypeDescriptor<Query> descriptor)
		// {
		// 	descriptor.Field(q => q.GetUsers(default!, default!, default!))
		// 			  .Authorize();
		// }
	}
}